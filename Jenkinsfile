pipeline {
    agent {
        node {
            label nodeLabel
            // customWorkspace URLDecoder.decode("/home/jenkins/workspace/${JOB_NAME}", "UTF-8")
        }    
    }
    stages {
        stage('show-job-parameters') {
            steps {               
                echo "OK"
            }
        }
    }
}
